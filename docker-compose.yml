version: '3.9'

services:
  ecommerce_app:
    container_name: ecommerce_app
    image: ecommerce_img/ecommerce_img
    build: .
    volumes:
      - .:/app
    ports:
      - "8005:8005"
    env_file:
      - .env
